services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        API_BASE_URL: ${API_BASE_URL:-http://localhost:5000}
    ports:
      - "5201:80"
    networks:
      - dotnetniger
    restart: unless-stopped

networks:
  dotnetniger:
    external: true
